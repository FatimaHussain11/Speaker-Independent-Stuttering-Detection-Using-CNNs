# Copy this file to `.env` and fill in your own local values.
# `.env` is git-ignored and must never be committed.

# Absolute or relative path to the root of your local copy of the
# dataset (the directory containing audio_dataset.csv and the raw
# audio files/folders). See data/README.md for the expected layout.
DATASET_ROOT=/path/to/dataset
